#include "Vision.h"

namespace simbio {
    namespace systems {

        void Vision::registerVisionSystem(
            flecs::world& world,
            std::vector<std::vector<organism::SimpleEntity>>& chunkGrid,
            int chunkSize,
            int chunkCols,
            int chunkRows)
        {

        }

    }
}
